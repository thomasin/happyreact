* All client side modules should be in dev dependencies not dependencies
* Avoid getting table names from query string or things that users can edit
* Always catch errors (send status and console log)
